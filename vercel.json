{
  "framework": "nextjs",
  "installCommand": "rm -f pnpm-lock.yaml && pnpm install --prefer-offline=false",
  "buildCommand": "NODE_OPTIONS='--max_old_space_size=4096' pnpm run build",
  "regions": ["iad1"],
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "crons": [
    {
      "path": "/api/feed",
      "schedule": "0 0 * * *"
    }
  ],
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "maxLambdaSize": "50mb",
        "memory": 4096,
        "nodeVersion": "18.x"
      }
    }
  ]
}
