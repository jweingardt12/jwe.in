{
  "framework": "nextjs",
  "buildCommand": "pnpm install --no-frozen-lockfile && next build",
  "outputDirectory": ".next/export",
  "regions": ["iad1"],
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "crons": [
    {
      "path": "/api/feed",
      "schedule": "0 0 * * *"
    }
  ]
}
